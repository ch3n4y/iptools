# 功能对照：原版《IP 地址修改器》5.0.5.8 → 本项目

对照依据：kn007 官方发布页（<https://kn007.net/topics/ip-address-modifier-5-0-remastered/>）的功能说明与
5.0.x 更新历史中列出的功能点。原版为 AutoIt 编写的单文件工具，本项目为 Tauri 2 + Rust + React 的全新实现，
**不使用原版代码、资源或配置格式**。

图例：✅ 已实现并验证 ｜ 🟡 已实现但交互方式不同 ｜ ➖ 有意不实现（含原因）

## 1. 网卡管理

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| 网卡列表：名称、MAC、系统序号 | `net::adapters`（`GetAdaptersAddresses` + `GetIfEntry2`）返回名称、描述、MAC、永久 MAC、序号、MTU、链路速度、媒体类型 | ✅ |
| 网卡状态图标（已连接 / 网线未连接 / 已禁用 / 网卡故障） | `AdapterStatus`：`connected / disconnected / disabled / faulty / unknown`，由 `OperStatus` + `MediaConnectState` + 设备 `ConfigFlags` 推导 | ✅ |
| 优先显示「已连接」网卡（有网关、系统序号靠前优先） | 列表排序：已连接 → 有默认网关 → IfIndex；首次进入默认选中该网卡 | ✅ |
| 刷新列表 / 刷新当前配置（后合并为一个操作） | 「刷新」按钮 + F5 + 可配置自动轮询（窗口隐藏时暂停）+ 窗口重新可见时刷新 | ✅ |
| 网卡启用 / 禁用（原版用 Setupapi.dll，报错时提示提升权限） | `SetupAPI`：`SetupDiSetClassInstallParamsW(DIF_PROPERTYCHANGE)` + `SetupDiCallClassInstaller`，全局与每配置两段式，与原版同样的失败提示（需管理员） | ✅ |
| 接口跃点数修改 | `GetIpInterfaceEntry` / `SetIpInterfaceEntry`（关闭自动跃点并写入指定值），读回校验 | ✅ |
| MAC 地址修改（写注册表，原版位于网卡驱动键） | 写 `…\Class\{4d36e972-…}\NNNN\NetworkAddress` 并按需重启设备使其立即生效 | ✅ |
| 随机生成 MAC（第一字节最低位为偶数以符合单播） | `net::random_mac()` 保留本地管理位、清除组播位 | ✅ |
| MAC 合法性检测（5.0.6.10 起加强） | 12 位十六进制、非全 0 / 全 FF、组播位不得置位，错误文案给出规则 | ✅ |
| 计算机名 / 工作组修改（Netapi32） | `SetComputerNameExW`（DNS 主机名 + NetBIOS）+ `NetJoinDomain`（工作组）；名称规则校验与「需重启生效」提示 | ✅ |
| 检测到新硬件自动刷新（5.0.5.1，实验性） | 定时轮询 + 可见性事件刷新；未做 WMI 硬件事件订阅 | 🟡 |
| WMI 出错时可选择关闭 / 重启 / 忽略 | 本项目改用 Win32 API（不依赖 WMI），错误以稳定错误码 + 详情呈现，可重试 | 🟡 |

## 2. IP 配置

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| 静态 IP / 子网掩码 / 默认网关 / 主备 DNS 修改 | `ApplyRequest` → `write::apply`：netsh（无 shell、按适配器名传参）→ 读回校验 → 失败回滚 | ✅ |
| 静态 ←→ 自动获取（DHCP）切换 | `source=static` / `source=dhcp`，切换前清理残留的手动地址，避免原版出现过的「IP 与掩码缺失」问题 | ✅ |
| 单网卡多 IP | `addresses[]` 多地址（界面建议 ≤6），主地址带网关，附加地址单独添加 | ✅ |
| IP / 掩码组合有效性判断（不能设为网络地址或广播地址） | `subnet::validate`：网络/广播地址、回环、组播、掩码连续性、重复填写 | ✅ |
| IP 是否被其他网卡占用检测（5.0.3.4） | 写入前扫描其他网卡的地址集合并给出占用者名称 | ✅ |
| 子网掩码标签双击切换常用 A/B/C 类掩码（5.0.5.8） | 双击「子网掩码」标签按 IP 首字节套用 /8 /16 /24 | ✅ |
| 默认网关标签双击按 IP 子网自动生成网关（5.0.5.8） | 双击「默认网关」标签或点「按子网推导」按钮（网络地址 +1；若与主机地址冲突则 +2） | ✅ |
| 网关与掩码对 IP 的校验（5.0.5.6） | 网关不在任何已配置子网内 → 告警；网关与主机地址相同 → 错误 | ✅ |
| DNS 地址超限导致蓝屏的修复（5.0.7.2） | 界面限制 3 条 DNS，逐条校验地址合法性 | ✅ |
| 修改后延迟获取网卡信息（避免读到 169.254） | 写入后轮询读回（最多 6 秒，400ms 间隔），只在校验通过后报告成功 | ✅ |
| 应用前提示与说明（5.0.8.3 补充交互提示） | 变更预览对话框：逐字段 from → to、告警、错误、需管理员提示 | ✅ |

## 3. 方案管理（原版「导入配置」）

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| 配置文件于程序根目录 `ip.dat`（INI） | JSON：`%APPDATA%\IP地址修改器\schemes.json`，便携模式（程序目录 `portable.txt`）时同目录 | 🟡 |
| 方案列表、双击应用、F6 应用选中方案 | 双击行、行内「应用」按钮、F6 | ✅ |
| 保存 / 另存配置（5.0.6.3 合并为一个按钮） | 「保存方案」（覆盖同名/同 id）+ 导出为 CSV / JSON | ✅ |
| 读取当前配置并保存为新方案（5.0.1.0） | 「从当前网卡新建方案」 | ✅ |
| 方案列表右键重命名、移动位置（5.0.6.5） | 重命名、上移 / 下移（`reorder_schemes`）、复制副本 | 🟡 |
| 从 Excel 导入配置（5.0.6.3） | `calamine` 解析 `.xlsx/.xls/.xlsm`；同时支持 CSV / TSV 粘贴导入；导出为 CSV 可再次导入（往返一致） | ✅ |
| 按 MAC 或计算机名匹配的方案优先显示（5.0.6.12） | `match_score`：MAC 100 分 / 主机名 60 分 / 网卡名 30 分，排序并显示「匹配当前网卡」标记 | ✅ |
| 方案保存前判断与说明、导入时的附加 IP 支持 | 导入报告逐行错误、跳过计数；合并策略（同名覆盖 / 仅新增）由用户选择 | ✅ |
| 方案携带单网卡多 IP（5.0.6.0 实验性） | 方案内含 `addresses[]` 多地址 | ✅ |
| 完全兼容旧 `ip.dat` | 不兼容（已与使用者确认） | ➖ |

## 4. 高级选项

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| 单网卡多 IP 设置（5.0.3.0 实验性） | 高级选项页地址行（≤6）+ 校验 + 应用 / 保存为方案 | ✅ |
| 自动根据 IP 子网生成默认网关（5.0.5.8 实验性） | 「按子网推导网关」 | ✅ |
| 子网标签切换常用 ABC 类掩码 | 掩码快捷按钮 | ✅ |
| 保存配置按钮（5.0.6.3） | 「保存为方案」 | ✅ |
| 危险/恢复类操作 | 启用禁用网卡、恢复为自动获取、恢复备份（均二次确认） | ✅ |

## 5. 工具箱

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| C 网群 Ping 器（基于 ARP） | `SendARP` 逐地址探测，可发现不回应 ICMP 的主机；结果带 MAC | ✅ |
| ICMP 模式（5.0.4.1） | `IcmpCreateFile` + `IcmpSendEcho`，适用于跨网段 | ✅ |
| 使用系统 `ping.exe`（5.0.8.0，5.0.8.1 修复准确性） | 系统 ping 模式（`-n 1 -w <ms>`，中英文输出均可解析延时） | ✅ |
| 慢速模式（5.0.7.4，针对限制设备的路由器/交换机） | 慢速开关：降低并发并在轮次间加延时 | ✅ |
| 多连发模式（5.0.7.6，结果更准） | 多连发轮次 1–10，合并统计最小/最大延时与丢包 | ✅ |
| 延时颜色区分（5.0.7.3，考虑轻微色弱） | 快 / 中 / 慢三档颜色 + 文字状态，颜色不是唯一信号 | ✅ |
| 本机 IP 颜色区分（5.0.8.0） | 本机地址行高亮 + 「本机」标记 | ✅ |
| 进度与取消 | 进度条、完成/在线计数、「停止」按钮 | ✅ |
| 结果导出 | 导出 CSV（带 BOM，Excel 可直接打开）+ 复制到剪贴板 | ✅ |
| 子网掩码计算器（5.0.4.0） | 掩码 / 反掩码 / 网络地址 / 广播地址 / 可用主机范围 / 可用主机数 / 地址类别 / 私有地址判定 | ✅ |
| 反掩码提示与双击复制（5.0.8.3） | 双击反掩码复制并给出提示 | ✅ |
| 复制主机范围（5.0.5.5 修正格式化） | 主机范围可一键复制 | ➕ |
| 「重置网络」（5.0.7.4 引入、5.0.7.5 移除，原版认为过于暴力） | 不实现 | ➖ |

## 6. 交互与其他

| 原版功能 | 本项目 | 状态 |
| --- | --- | --- |
| F1 帮助（含大量使用说明） | 内置帮助页：快捷键、使用流程、功能逐条说明、常见问题 | ✅ |
| F8 工具箱 | F8 跳转群 Ping 器（工具箱应用在同一侧栏） | ✅ |
| F12 打开系统「网络连接」 | `control ncpa.cpl` | ✅ |
| 最小化窗口 | 无边框标题栏的最小化 / 最大化 / 关闭 | ✅ |
| 状态栏显示版本号 | 标题栏与状态栏均显示版本；状态栏另显示网卡计数、当前网卡、配置目录 | ✅ |
| 状态栏双击访问软件发布页 | 改为「配置目录」按钮（避免无意打开外部浏览器）；更新检查在设置页 | 🟡 |
| 窗口渲染顺序与响应速度优化 | 前端骨架 + 只更新变化的状态；网卡轮询可关闭 | 🟡 |
| requireAdministrator 清单 | 一致（`windows-app-manifest.xml`），未提权时给出提示与「以管理员身份重启」 | ✅ |
| 内部错误输出日志 | 未写日志文件；错误以稳定错误码 + 技术详情折叠呈现（可用命令行模式导出 JSON 诊断） | 🟡 |
| 代码混淆与加壳（降低误报） | 不实现：不混淆、不加壳，源码公开 | ➖ |
| 自动更新 | 原版无；本项目经 GitHub Releases + Tauri updater 提供签名校验的自动更新 | ➕ |
| 托盘常驻、开机自启 | 原版无，本项目亦无 | ➖ |

## 7. 技术实现差异（供排查参考）

| 原版 | 本项目 |
| --- | --- |
| WMI（`Win32_NetworkAdapter(Configuration)`）、`Netapi32.dll`、`Setupapi.dll`、注册表 | `GetAdaptersAddresses` / `GetIfEntry2` / `GetIpInterfaceEntry` / `SendARP` / `IcmpSendEcho` / SetupAPI / `SetComputerNameExW` / `NetJoinDomain` + 注册表（用 `winreg`）；写入经 `netsh`（无 shell 调用，按适配器别名传参，规避本地化差异） |
| AutoIt 单文件脚本 | Rust 后端 + React 前端；配置为 JSON；错误为稳定错误码 |
| 读写配置在程序目录 | 默认 `%APPDATA%`，可选便携模式 |
| 无自动化测试 | Rust 29 项单测（子网计算、配置校验、方案迁移、CSV/Excel 解析、群 Ping 聚合、网卡规则）+ 前端 Vitest |

> 差异补充：本项目不提供原版 `ip.dat` 的导入；如需要迁移既有方案，请按
> `docs/CONTRACT.md` 中的 CSV 表头整理为 CSV/Excel 后导入。
